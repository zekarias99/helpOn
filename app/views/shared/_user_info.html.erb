<%#= =========here goese avatar link=========   %>
<h1>
  <%= current_user.full_name %>
</h1>
<span>
  <%= link_to "view my profile", current_user %>
</span>
<span>
  <%= pluralize(current_user.statuses.count, "status") %> 
</span>