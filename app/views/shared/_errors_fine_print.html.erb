<% if @fine_print.errors.any? %>
<section id="errors">
  <header>
    <h2>
      Oops! The <%= @fine_print.class.name.titleize.downcase %> could not be saved.
    </h2>
    <h3>
      Please correct the following <%= pluralize(@fine_print.errors.count, "error") %>:
    </h3>
  </header>
  <ul>
    <% @fine_print.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
</section>
<% end %>