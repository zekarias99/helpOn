<%= form_for(@status) do |f| %>
      <%= render "shared/error_messages", object: @status %>
<div class="field">
<%= f.text_area :content, placeholder: "Compose mew state..." %> 
</div>
 <%= f.submit "Post", class: "btn btn-large btn-primary" %>
<% end %>
