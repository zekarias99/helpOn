<%#= I just created this depend on the book and take a look on this code %>
<tr>
	<td><%= line_item.quantity %>&times;</td>
	<td><%= line_item.deal.blurb_title %></td>
	<td ><%= number_to_currency(line_item.total_price) %></td>
</tr> 